const moment = require("moment"); module.exports = { config: { name: "up", aliases: ["stayup", "alive"], version: "1.1", author: "Camille & Hermann", countDown: 5, role: 0, shortDescription: "Empêche le bot de s'éteindre (silencieusement)", longDescription: "Active un keep-alive invisible pour que le bot reste allumé sans envoyer de messages.", category: "system", guide: { fr: "{p}up" } }, onStart: async function({ api, event }) { const startTime = moment().format("HH:mm:ss"); api.sendMessage(`✅ Mode anti-off silencieux activé à ${startTime}\nLe bot restera actif en continu.`, event.threadID); // 🔄 Keep Alive invisible (ping interne toutes les 5 min) setInterval(() => { console.log("🟢 Keep Alive silencieux:", new Date().toLocaleTimeString()); api.getCurrentUserID(); // ping silencieux }, 5 * 60 * 1000); // toutes les 5 minutes } };
